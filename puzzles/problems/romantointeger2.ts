

// MCMXCIV
// solving in my head: what do I do??
// [1000] CMXCIV 
// [1900]  XCIV // NOTICE: I basically counted CM as a single "number" together in one step.
// [1990]  IV   // again did two
// [1994]       // again did two

function romanToInt(s: string): number {

    let index = 0

    while (index < s.length) {

        if ((s[index] == "C" || "X" || "I") && s[index + 1] == "") {

        }

    }

}